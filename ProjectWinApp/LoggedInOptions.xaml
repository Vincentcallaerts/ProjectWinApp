<Window x:Class="ProjectWinApp.LoggedInOptions"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectWinApp"
        mc:Ignorable="d"
        Title="LoggedInOptions" Height="450" Width="800">
    <Window.Resources>
        <!--A Style that affects all TextBlocks-->
        <Style TargetType="Button">
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Margin" Value="50,10,50,0"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock VerticalAlignment="Top" x:Name="tbInfo"></TextBlock>
            </StatusBarItem>
        </StatusBar>
        <StackPanel DockPanel.Dock="Top" >
            <Label HorizontalAlignment="Center" Margin="0,10,0,10" BorderBrush="Black" BorderThickness="1">Welkom</Label>
            <Button>DataBeheer</Button>
            <Button>Overzicht</Button>
            <Button>Bestellingen</Button>
        </StackPanel>
        
    </DockPanel>
</Window>
